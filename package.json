{
  "name": "accesspack",
  "version": "0.1.0",
  "private": true,
  "description": "Node helper: run axe-core against fetched HTML via jsdom",
  "scripts": {"scan": "node scripts/scan.js"},
  "engines": {"node": ">=18"},
  "dependencies": {"axe-core": "^4.10.3", "jsdom": "^26.1.0"}
}
